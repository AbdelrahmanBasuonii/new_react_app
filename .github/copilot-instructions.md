# Fullstack Portfolio Lab

- React and Vite are used for the frontend.
- Keep the six-page navigation in `src/App.jsx` and reusable visual rules in `src/styles.css`.
- Run `npm run dev` for local development and `npm run build` to verify production output.
- Preserve the interactive calculator, store cart, and dashboard filters when changing the project lab.
